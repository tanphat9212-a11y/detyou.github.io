import { products } from "./data/products.js";

export function fetchProducts(){
  return new Promise((resolve)=>{
    setTimeout(()=>{
      resolve(products);
    }, 800); // giả lập server delay
  });
}